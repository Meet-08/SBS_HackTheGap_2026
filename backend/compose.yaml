services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=sbs'
      - 'POSTGRES_PASSWORD=1234'
      - 'POSTGRES_USER=meet'
    volumes:
      - pg_data:/var/lib/postgresql
    ports:
      - '5432:5432'

volumes:
  pg_data: